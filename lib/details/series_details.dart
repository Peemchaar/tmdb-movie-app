import 'package:flutter/material.dart';

class SeriesDetails extends StatelessWidget {
  var tvId;
  SeriesDetails({super.key,  required this.tvId});

  @override
  Widget build(BuildContext context){
    return Container();
  }
}